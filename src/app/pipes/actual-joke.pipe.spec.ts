import { ActualJokePipe } from './actual-joke.pipe';

describe('ActualJokePipe', () => {
  it('create an instance', () => {
    const pipe = new ActualJokePipe();
    expect(pipe).toBeTruthy();
  });
});
